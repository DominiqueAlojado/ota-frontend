/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "webdevelopmenthub.b-cdn.net",
      "bx-branding-gateway.cloud.seek.com.au",
    ],
  },
};

export default nextConfig;
